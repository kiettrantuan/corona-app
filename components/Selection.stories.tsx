import * as React from 'react';
import Selection from './Selection';

export default {
    title: 'App/Selection',
    component: Selection
};

export const Main = () => {
    return (
        <Selection/>
    )
}